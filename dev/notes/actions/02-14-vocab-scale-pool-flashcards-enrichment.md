Hey, let's discuss this. I actually want you to create a PRD doc for this or something like that in your notes, thoughts, thinking document. We'll be discussing two systems:
1. Right now, we have 150 words in our pool. I want to have much, much more. Imagine 1000 or 10,000 words in the database.
All our systems are not ready for that. There are three specific things they have in mind:
1. Building the world word pool set specifically. If we have 10,000 words, they want to pick just random words from all over the set. That's not what they want. Instead, they want some gradual controls over the pool and mechanism number one that they see is the word sets. Use activated word sets to form the pool, not just all the words in the dictionary. When we change the activated word sets, we'll regenerate the pool.
2. Having some progression mix complexity mix. Have some complexity and frequency for the words because if you look at our word set generator, it has this complexity parameters and all of that.
Another thing I wanted to ask you is, when we install the app, what happens? Do we load the data from JSON? What I want to do is load the data from to generate everything with our Python script. I wonder if we have to generate it like pre-generated and save all 10,000 words to the assets, listing some in maybe in the JSON, or is it too much? It's like maybe 10 MB, maybe 100 MB. Do we care? It's small, I don't really care. Because it's better to pre-generate it and be able to verify it rather than have to install the whole Python environment on every user desktop.

System number two is the Flashcards. Right now, we don't have all the Flashcards, and we're not going to be having 10,000 Flashcards in the app, in the database. I'm not sure. I don't want to generate all of them right away. Instead, I imagine we could generate them on the fly. At least one user generates it locally on our own website, and we save it and we have it and don't have to generate again. That will gradually use our token quota and everything, not in a huge one drop, and maybe make one mistake, and everything is broken.

System number three is how do we add all the words, the definitions. Actually, I was thinking to make a system like generate the definitions and generate the A.I. stuff. Well, definitions translations maybe like the data model. Let's say like user adds a new word and they just print an English value, then we can generate the corresponding translations and descriptions with A.I. local model or API and show them to verify they click OK and then it's done.

prd: scale vocabulary + pool + flashcard generation + ai enrichment to 10k words
users: primary maintainer, family user, future web/admin maintainer
success:
- app works with 10,000+ canonical words without UX degradation
- active word sets drive pool formation (never random over full dictionary by default)
- flashcards are generated lazily and cached
- user-added words can be enriched (translation/definition) with human approval
non-goals:
- generating all flashcards upfront
- requiring python runtime on end-user machines
- full supabase dependency for offline core flows

current state snapshot (code):
- bundled canonical catalog loads from `/Users/petrlavrov/work/archive/baby_keyboard/BabyKeyboardLock/Resources/word_sets.json` via `/Users/petrlavrov/work/archive/baby_keyboard/BabyKeyboardLock/utils/WordRepository.swift`
- local catalog mirror override exists: `/Users/petrlavrov/work/archive/baby_keyboard/BabyKeyboardLock/utils/WordCatalogStore.swift` (`Application Support/BabyKeyboardLock/word_catalog.json`)
- active sets -> runtime words: `/Users/petrlavrov/work/archive/baby_keyboard/BabyKeyboardLock/utils/RandomWordList.swift` (`var words`)
- learning pool is rebuilt from learning words synced from enabled sets (+custom image words + baby name), not full dictionary

decisions:
- keep pre-generated catalog JSON as install-time source of truth (generated by python offline before release)
- ship large catalog in app bundle (10MB-100MB is acceptable for this product)
- no python on user desktop for normal usage
- keep offline-first; optional cloud sync/generation later

system a: large vocabulary + controlled pool
- data packaging:
- generate canonical catalog offline with scripts (`entries`, `sets`, metadata)
- ship as bundled JSON
- optional split by shard later if startup becomes slow
- pool formation:
- source universe = enabled sets only
- on enabled-set change: force pool regeneration immediately
- keep existing daily stale refresh behavior
- progression controls to add:
- per-word metadata fields in catalog:
- `frequency_rank` (int)
- `complexity_level` (enum: starter/easy/medium/advanced)
- optional `age_band` / `curriculum_stage`
- pool filter config:
- allowed complexity range
- target frequency window (common-first, balanced, rare boost)
- learning sampler then operates inside filtered set
- persistence:
- config in user defaults (or settings store)
- catalog remains canonical JSON + local override

system b: flashcards at scale (lazy generation)
- asset strategy:
- do not pre-generate 10,000 images
- generate on demand when word first appears without local image
- cache key:
- `word_id + style + language + prompt_version + model`
- cache layers:
- local disk cache first
- optional remote store (website/supabase bucket) second
- queue behavior:
- background queue with concurrency limit + retry
- placeholder image while pending
- manual regenerate action for a single word
- quota safety:
- daily/monthly generation budget
- dedupe checks before generating

system c: user-added word enrichment (ai-assisted, human-approved)
- flow:
- user enters english spelling (+ optional meaning key)
- app generates draft translations + definitions (+ optional tags/category)
- user reviews diff, edits, confirms
- on confirm: persist into local catalog mirror and optionally enqueue flashcard generation
- providers:
- local model if configured, else API provider
- strict timeout + fallback + error state in UI
- safety:
- no auto-commit without user confirmation
- provenance fields (`generated_by`, `generated_at`, `provider`, `prompt_version`)

install/init behavior (explicit):
- on install, app loads bundled JSON catalog
- if local mirror exists, local mirror overrides bundled for runtime
- reset action deletes local mirror and reloads bundled JSON
- python generation happens only in maintainer pipeline before release

rollout plan:
- phase 1: schema extension in catalog + script support (`frequency_rank`, `complexity_level`)
- phase 2: add settings controls for complexity/frequency filters and wire pool builder
- phase 3: implement lazy flashcard generation queue + local cache manifest
- phase 4: implement add-word enrichment draft/approve flow
- phase 5: optional remote sync + shared cache

acceptance criteria:
- can load and interact with 10,000-word catalog with no crashes and acceptable startup time
- pool always derives from enabled sets + active filters
- daily stale pool refresh works even if machine was off at prior refresh timestamp
- first-time flashcard generation works for uncached words and is reused from cache afterward
- user-added word enrichment can be confirmed/rejected and persists correctly

open questions:
- target startup budget on older macbooks (p50/p95)
- where to store flashcard cache manifest (sqlite vs jsonl)
- remote cache ownership/auth model when web + desktop share assets
